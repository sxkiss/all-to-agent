# 服务配置
HOST=0.0.0.0
PORT=8000

# 助理名称
ASSISTANT_NAME=小助手

# CLI 工作目录（CLI 执行工具时的 cwd，默认用户主目录）
# WORK_DIR=/home/sxkiss

# Claude CLI 模型（可选，留空用 CLI 默认模型）
# CLAUDE_MODEL=sonnet

# CLI 超时（秒）
CLI_TIMEOUT=300

# 最大对话轮次
MAX_TURNS=10
